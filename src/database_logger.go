package logger

// DatabaseLogger represents a logger that
// logs messages to a database

type DatabaseLogger struct {}

func (dl *DatabaseLogger) Log(message string) {
    // Implementation for logging to database
}
